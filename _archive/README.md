# Archive — code legacy non utilisé en production

Le site SURO est déployé en **statique (GitHub Pages) + Supabase**. Ces dossiers ne sont plus chargés par le front.

| Dossier | Raison de l'archivage |
|---------|----------------------|
| `backend/` | Ancien serveur Express (autre projet Supabase). Remplacé par Supabase direct + Edge Functions. |

Pour la tarification admin, l'écran legacy reste temporairement dans `backoffice/` (lien depuis ops Paramètres).
